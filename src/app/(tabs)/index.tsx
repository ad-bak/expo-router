import { Link } from "expo-router";
import { Text } from "react-native";

export default function About() {
  return <Text>This is a demo app for the Expo Router library. You can find the source code for this app at.</Text>;
}
